# Shared Package Workspace

This folder is reserved for future shared models and helpers if the project grows into:

- a dedicated front end
- a second client surface
- a richer server domain model

Current MVP does not require a shared package yet.
