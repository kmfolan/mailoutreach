# Mobile Workspace

This folder is reserved only if Outbound Forge eventually needs a mobile operator app.

Current product focus:

- one protected web app
- one Node server
- one deployment path

Do not build mobile first unless the web deployment is already stable and in active use.
