# Web Workspace

This folder is intentionally reserved for a future structured web client.

Current source of truth for the running MVP:

- root `index.html`
- root `styles.css`
- root `app.js`
- `server/src/index.js`

Do not use this folder as the live app surface unless the project is intentionally migrated into it.
